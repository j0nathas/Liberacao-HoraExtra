// Exemplo de configuração com pg (node-postgres)
// Instale: npm install pg
// const { Pool } = require('pg')
// const pool = new Pool({ connectionString: process.env.DATABASE_URL })
// module.exports = pool

module.exports = {} // substitua pela configuração real do seu banco
